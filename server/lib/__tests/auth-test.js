/**
 * Tests for the Auth module.
 */

describe('Auth module', () => {
    describe('AuthenticatateUser', () => {

        it('Should work', (done) => {
            expect(2).to.equal(2);
            done();
        });

    });

});

